# Mongo
MONGO_URI=mongodb://localhost:27017
MONGO_DB=mongo_tic_tac_db

# Postgres
POSTGRES_HOST=localhost
POSTGRES_PORT=5433
POSTGRES_DB=postgres_tic_tac_db
POSTGRES_USER=postgres_tic_tac_user
POSTGRES_PASSWORD=postgres_tic_tac_password # Change to something stronger

# MinIO
S3_ENDPOINT=http://localhost:9000
S3_ACCESS_KEY=minio
S3_SECRET_KEY=minio123 # Change to something stronger
S3_BUCKET=models
S3_REGION=eu-west-3 # Not needed for MinIO but would allow for AWS if desired